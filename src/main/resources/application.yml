spring:
  application:
    name: mcsv-login
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect
  datasource:
    url: jdbc:mysql://localhost:3306/newdata?useSSL=false&serverTimezone=UTC
    username: root
    password: root